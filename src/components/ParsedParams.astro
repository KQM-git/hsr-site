---
import { parseParams } from "util/parseParams";

export interface Props {
  data: string;
  params: number[];
  className?: string;
}

const { data, params } = Astro.props;

const html = parseParams(data, params);
---

<Fragment set:html={html} />
